# UI 설계 기록 · M5-08에서 먼저 정하고 M6-01에서 구현

> 다른 서비스의 로고·글꼴·이미지·화면을 복제하지 않습니다. 공개 자료는 **정보 위계와 상호작용 원칙**만 참고하고, 내 데이터·내 문장·원본 CSS/SVG로 만듭니다.
> 2026-09-19 작성. 이 문서는 설계이며 아직 구현하지 않았습니다. 화면 문장은 `requirements.md`의 문구를 그대로 씁니다.

## 1. 참고 원칙과 근거

`docs/ui_ref.md`에 정리된 원칙 3가지만 가져왔습니다. 참고 링크는 `ui_ref.md`에 있는 것이며 이번에 직접 열어 보지는 않았습니다. 관찰용 화면은 2026-09-19 수강생이 보내 준 캡처 1장(공공 입찰공고 목록 화면)이며 2-2절에 기록했습니다.

|참고한 원칙|내 화면에서의 적용|참고 링크|
|---|---|---|
|한 화면·섹션에 한 메시지|랜딩 섹션마다 제목 한 줄과 행동 하나. 앱은 첫 화면에서 「조건을 넣고 공고 찾기」 행동 하나만 보인다.|`docs/ui_ref.md`|
|핵심 숫자는 크게|목록에서 가장 큰 글자는 「개찰까지 D-22」이고 공고명·금액은 그다음 크기로 한다. 랜딩 데이터 카드는 「134건 중 3건」과 「중앙값 6일」을 크게 쓴다.|`docs/ui_ref.md`, `data_sources.md`|
|누를 것은 크고 분명하게 · 쉬운 순서로 묻기|버튼은 동사로 하나씩(「공고 찾기」「메일 본문 복사」「공고 찾기 시작」). 입력 순서는 사용자가 먼저 아는 값부터: 키워드 → 금액 범위 → 기준일.|`docs/ui_ref.md`, `journey_map.md`|
|오류는 입력 가까이|R-02·R-03a 안내를 해당 입력 칸 바로 아래에 빨간 글씨로 표시한다.|`requirements.md`|

## 2. 랜딩 시각 설계 (M6-01에서 `index.html`로 구현)

색은 두 가지만 쓴다(짙은 청록 1개 + 강조 주황 1개, 배경은 흰색·연한 회색). 참고한 화면의 파란 계열과 겹치지 않게 청록을 쓴다. 글꼴은 시스템 기본 글꼴이고 외부 글꼴·CDN·이미지 파일은 쓰지 않는다. 그림은 CSS 도형과 원본 SVG로 만든다.

|랜딩 요소|무엇을 보여 주나|만드는 방법|원천|
|---|---|---|---|
|Hero|「부산지역 교육 및 학교(어린이 시설 등) 관련 입찰공고 찾기」 + 한 줄 가치 「조건으로 걸러 보고, 고른 공고는 메일 본문으로 바로 복사하세요.」 + 큰 버튼 「공고 찾기 시작」|CSS 도형 + 간단한 SVG 아이콘|`lean_canvas.md` 칸 3(UVP)|
|데이터 카드·미니 차트|큰 숫자 2개: 「134건 중 3건」(교육·어린이 관련) · 「6일」(소액수의견적 공고→개찰 중앙값). 작은 글씨로 기관·기간·한계. 미니 차트: 관련 3건의 공고→개찰 기간 막대(6일·22일·55일)|CSS 카드 + SVG 막대 3개|`data_sources.md` D1·D2|
|전·후 흐름|기존(월·목 사이트 순회 → 화·금 옮겨 적어 메일) → F-01(조건 입력 → 목록 → 본문 복사) → 결과(동료·직원에게 보낼 메일 본문). 시간이 얼마나 줄어든다는 문구는 쓰지 않는다(미측정 `[가설]`)|3단계 화살표 도형|`journey_map.md`, `persona.md`|
|앱 미리보기|R-01 정상 화면(「어린이」 검색 결과 2건)을 그린 원본 화면|`app.html`의 결과 화면을 축소해 넣거나 자체 도형|`requirements.md` R-01|
|신뢰·한계|「수업 과제 데모 · 부산도시공사 2025년 공고 134건 · 실시간 공고가 아님 · 개찰예정일은 접수 마감과 다를 수 있어 공고 원문(나라장터) 확인 필요 · 교육청 등은 링크로만 안내」|작은 고지 카드|`feedback_log.md`, `business_plan.md` ⑧ 한계|

## 3. 앱 화면 설계 (`app.html`)

- **첫 화면에서 사용자 행동 하나만 보이게**: 「조건을 넣고 [공고 찾기]」. 목록·안내는 처음에 비어 있다.
- **입력 순서와 각 입력의 예시**
  0. 자료 — (`data_pilot.js`가 있는 내 PC에서만 보임) 라디오: 「부산도시공사 2025년 공고(공개)」(기본) / 「기존 수집 자료(비공개)」
  1. 키워드 — 예: `어린이, 강사, 교육` (쉼표로 여러 개, 「교육」은 동의어로 함께 찾음)
  2. 최소 금액(원) 이상 — **기본값 `5,000,000`이 미리 채워짐**(비우면 최소 제한 없음) · 최대 금액(원) 이하 — 비워 두면 **제한 없음(무한대)**. 예시는 `100,000,000` (쉼표 허용)
  3. 기준일 — 기본값은 **오늘 날짜**, 설명 「개찰까지 남은 일수를 이 날짜 기준으로 계산합니다. 기본값은 오늘 날짜입니다.」 (공개 자료가 2025년 공고라 기본 기준일에서는 모두 「개찰 지남」으로 보인다)
  4. 체크 「마감·개찰 지난 공고 포함」 (기본 꺼짐 = 지난 공고는 뺌, 켜면 바로 다시 찾음)
  5. 버튼 「공고 찾기」(채운 버튼)와 「조건 지우기」(테두리 버튼) (Enter 키로도 실행)
- **정상 결과(R-01)에서 크게 보여 줄 값**: 각 공고의 「개찰까지 D-22」. 요약 「조건에 맞는 공고 2건 (기준일 2025-11-10)」은 그다음 크기, 공고명·금액·날짜는 보통 크기.
- **빈값(R-02)과 오류(R-03) 안내 문구**: `requirements.md` 표의 문장을 그대로 쓴다(빈값 1개, 오류 4개+자료에 없음 1개+불러오기 실패 1개, 메일 본문 안내 3개).
- **키보드·모바일에서 눌러 볼 요소**: Tab 순서는 키워드 → 최소 → 최대 → 기준일 → 공고 찾기 → 목록 체크 → 메일 본문 복사 → 메일 작성 창 열기. 각 입력에 `<label>`을 붙이고 오류 문구는 `role="alert"`로 알린다. 폭 360px에서 한 열로 쌓이고, 목록은 표 대신 카드로 바뀌며 버튼은 손가락으로 누르기 쉬운 크기(높이 44px 이상)로 한다.

### 3-1. 상태별 와이어프레임 (텍스트로 그린 원본 설계)

> 2026-09-19: 참고 화면을 반영한 최종 배치(조건 패널·툴바·표 구조)는 **3-3절이 우선**한다. 아래 와이어프레임은 상태별 문구를 보여 주는 용도다.

**R-01 정상** — 키워드 「어린이」, 기준일 2025-11-10

```
┌───────────────────────────────────────────────┐
│ 부산지역 교육 및 학교(어린이 시설 등) 관련 입찰공고 찾기              │
│ 키워드   [ 어린이                    ]          │
│ 최소 금액 [        ]  최대 금액 [        ]      │
│ 기준일   [ 2025-11-10 ]  ← 개찰까지 일수 기준일 │
│ [ 공고 찾기 ]                                   │
│ 출처: 부산도시공사 입찰공고(공공데이터포털)…     │
├───────────────────────────────────────────────┤
│ 조건에 맞는 공고 2건 (기준일 2025-11-10)         │
│ ☐  D-22   EDC 2단계 2공구 어린이공원 놀이시설…  │
│           물품(내자) · 게시 2025-11-10 · 개찰 2025-12-02 │
│           345,454,545원                         │
│ ☐  개찰 지남(13일 전)  부산도시공사 직장어린이집…│
│           기술용역 · 게시 2025-09-03 · 개찰 2025-10-28 │
│           100,584,000원                         │
│ [ 메일 본문 복사 ]  [ 메일 작성 창 열기 ]        │
└───────────────────────────────────────────────┘
```

**R-02 빈값** — 키워드·금액 모두 비움

```
│ 키워드   [                          ]          │
│ ⚠ 키워드나 금액 범위 중 하나 이상을 입력해 주세요. 예: 키워드에 어린이 │
│ 최소 금액 [        ]  최대 금액 [        ]      │
│ [ 공고 찾기 ]     (목록·요약 없음, 이전 결과 지움) │
```

**R-03a 잘못된 입력** — 최소 금액 「abc」

```
│ 최소 금액 [ abc     ]  최대 금액 [        ]      │
│ ⚠ 금액은 숫자로 입력해 주세요. 예: 100,000,000   │
│ (목록·요약 없음, 이전 결과 지움)                  │
```

**R-03b 자료에 없음** — 키워드 「수학여행」

```
│ ┌ 조건에 맞는 공고가 이 자료에 없습니다. ─────┐ │
│ │ 이 자료는 부산도시공사 2025년 공고 134건뿐입니다. │ │
│ │ 키워드를 줄이거나 금액 범위를 넓혀 보세요. …    │ │
│ │ 다른 곳에서 직접 확인하기                       │ │
│ │  · 부산교육청 입찰공고 안내 (학교장터·나라장터 연결) ↗ │ │
│ │  · 부산교육청 계약체결현황 ↗                    │ │
│ └───────────────────────────────────────────┘ │
```

**R-03c 자료 불러오기 실패** — `data.js` 없음

```
│ ⚠ 공고 자료를 불러오지 못했습니다. data.js 파일이 app.html과 같은 폴더에 있는지 확인해 주세요. │
│ [ 공고 찾기 ]  ← 눌러지지 않음(비활성)            │
```

**R-04 메일 본문** — 선택 후 복사

```
│ ☑  D-22  EDC 2단계 2공구 어린이공원 놀이시설…   │
│ [ 메일 본문 복사 ]                              │
│ ✔ 메일 본문을 복사했습니다. 메일에 붙여넣기(Ctrl+V) 하세요. │
│ (복사가 막히면) 자동 복사가 안 됩니다. 아래 본문을 직접 선택해 복사해 주세요. + 본문 텍스트 상자 │
```

### 3-2. 화면 요소 ↔ 요구사항 ↔ 확인 방법

|화면 요소|연결된 R-ID|확인 방법 (`qa_scenarios.md`)|
|---|---|---|
|키워드·금액·기준일 입력 칸과 「공고 찾기」 버튼|R-01·R-02·R-03a|IT-01a·IT-01c·IT-02·IT-03a|
|결과 요약 「조건에 맞는 공고 N건 (기준일 …)」|R-01|IT-01a·IT-01b|
|목록의 「개찰까지 D-22」 (가장 큰 글자)|R-01|IT-01a (UT-01d·UT-01e)|
|「교육」 동의어 안내 한 줄|R-01|IT-01b|
|추정가격 표시(과학 표기 행은 「원본 표기 확인 필요」)|R-01|UT-01f|
|입력 칸 아래 빨간 오류 문구|R-02·R-03a|IT-02·IT-03a|
|「자료에 없음」 안내 상자와 교육청 링크 2개|R-03b|IT-03b|
|불러오기 실패 안내와 비활성 버튼|R-03c|IT-03c|
|목록 체크 상자·「메일 본문 복사」·「메일 작성 창 열기」와 안내 문구|R-04|IT-04 (UT-04a·b)|
|출처·기준 시점 문구(입력 아래·목록 위·맨 아래·메일 본문 끝)|R-01·R-04|IT-01a·IT-04|
|「마감·개찰 지난 공고 포함」 체크(기본 꺼짐)|R-01|IT-01d (UT-01g·UT-05h)|
|「자료」 선택 라디오(내 PC에서만 보임)|R-05|IT-05a·IT-05b (UT-05a)|
|상태 칩(공고·수정·재공고·취소·결과)과 이력 줄|R-05|IT-05a (UT-05b)|
|담당자·연락처 줄(기존 수집 자료)|R-05|IT-05c (UT-05f)|
|「마감일 미상」·「금액 미상」 표시|R-05|UT-05c·UT-05d|

### 3-3. 참고 화면을 반영한 최종 배치 (M6-02에서 `app.html`로 구현)

관찰한 화면(2-2절)의 정보 위계 원칙을 내 데이터·내 문장으로 다시 그렸다. 위에서 아래로 「조건 → 결과 요약·행동 → 목록」 순서다.

```
┌ 머리띠: 부산지역 교육 및 학교(어린이 시설 등) 관련 입찰공고 찾기 (짙은 청록, 메뉴 없음) ─────┐
├ 조건 패널(카드) ─────────────────────────────────────────────┤
│ 자료   (●) 부산도시공사 2025년 공고(공개)  ( ) 기존 수집 자료(비공개) ← 내 PC에서만
│ 키워드 [ 어린이, 강사, 교육          ]  최소 [5,000,000]원  최대 [제한 없음]원
│ 기준일 [ 2025-12-23 ]  [ ] 마감·개찰 지난 공고 포함
│ 출처: 부산도시공사 입찰공고(공공데이터포털) · …
│                                [ 조건 지우기 ]  [ 공고 찾기 ]
├ 결과 툴바 ───────────────────────────────────────────────────┤
│ 조건에 맞는 공고 2건 (기준일 2025-11-10)      [ 메일 본문 복사 ] [ 메일 작성 창 열기 ]
├ 목록(카드형 표) ─────────────────────────────────────────────┤
│ ☐ | 마감까지 |  공고명(링크)                 | 기관·구분   | 게시일 → 마감일 | 금액
│ ☐ | D-22     |  EDC 2단계 2공구 어린이공원…  | 물품(내자)  | 2025-11-10 → 2025-12-02 | 345,454,545원
│   담당자 … · 연락처 …  (기존 수집 자료일 때만)  ·  이력: 공고 → 수정 …
└───────────────────────────────────────────────────────────┘
```

|참고 화면에서 가져온 원칙|내 화면의 적용|
|---|---|
|검색 조건을 표 바로 위 한 묶음으로 둔다|조건 패널 카드 하나에 자료·키워드·금액·기준일·체크를 모으고 결과는 그 아래에만 나온다|
|주 행동 버튼은 채우고 보조 버튼은 테두리로 구분한다|「공고 찾기」는 청록 채움, 「조건 지우기」는 테두리만|
|진행 중만 보는 체크를 조건 줄에 둔다|「마감·개찰 지난 공고 포함」을 기준일 옆에 둔다(기본 꺼짐 = 진행 중만 보임)|
|한 칸에 게시일과 마감일을 함께 보여 준다|「게시일 → 마감일」 한 칸으로 붙이고, 가장 큰 글자는 「마감까지 D-22」|
|공고명은 링크로, 진행 상태는 칩으로 보여 준다|기존 수집 자료는 공고명이 원문 링크이고 상태 칩(공고·수정·재공고·취소·결과)을 붙인다(도시공사는 원문 링크가 없어 글자만)|
|결과 위 툴바에 내보내기 같은 행동을 모은다|「메일 본문 복사」「메일 작성 창 열기」를 결과 요약 오른쪽에 둔다(엑셀 내려받기는 F-02 이후)|

## 2-1. AIDA 랜딩 구조 · 발표도 같은 순서로

|AIDA|랜딩에서 보여 줄 것|반드시 연결할 문서|사용자가 얻는 답|
|---|---|---|---|
|A · Attention|제목 「부산지역 교육 및 학교(어린이 시설 등) 관련 입찰공고 찾기」 + 문제 한 줄 「소액 공고는 공고 뒤 중앙값 6일이면 개찰됩니다」 + 숫자 2개(134건 중 3건 · 6일)·기간·출처|`topic.md`, `data_sources.md`|왜 지금 봐야 하나|
|I · Interest|페르소나 카드: 「ijw 대표 · 40대 · 부산 소규모 교육기업 대표 겸 입찰 담당」 + 불편 순간(월·목에 검색하고 화·금에 메일로 배포하다 마감 뒤에야 알게 되는 경우) + 현재 대안. 인용 문장은 후기처럼 보이지 않도록 **「페르소나」 라벨**을 붙인다|`persona.md`, `journey_map.md`|누구의 어떤 문제인가|
|D · Desire|해결 원리(조건으로 걸러 개찰까지 남은 일수를 함께 보고, 메일 본문을 바로 복사) + 전·후 3단계 흐름 + 가치 한 줄. 시간 절감 수치는 쓰지 않는다|`lean_canvas.md`, `mvp_rice.md`|왜 이 방법이 나은가|
|A · Action|앱 미리보기(R-01 화면) + 큰 버튼 「공고 찾기 시작」 → `app.html`|`requirements.md`, `ui_design.md`|지금 무엇을 해 볼 수 있나|

- 타깃은 Interest 영역에서 직업·상황·계기로 한 사람을 먼저 보여 줍니다.
- 기능 소개는 Desire와 Action에 나눕니다. Desire는 **해결 원리**, Action은 **직접 누를 F-01**입니다.
- 마지막에는 데모 범위·출처·한계를 작게 표시합니다(테스트 통과 수는 실제 `test_log.md` 결과가 생긴 뒤에만 쓴다).

## 2-2. 관찰용 레퍼런스 캡처 기록

|캡처 파일·원본 URL|관찰한 원칙|복제하지 않을 요소|내 AIDA 화면에 적용한 원본 설계|
|---|---|---|---|
|**수강생이 대화로 보낸 캡처 1장** (공공 입찰 시스템의 「입찰공고 목록」 화면, 화면 시각 2026-09-19 14:20). 파일(`references/ui_ref_01.png`)로는 저장하지 않았고 GitHub·Vercel·패들렛에 올리지 않는다|① 검색 조건을 표 위 한 묶음으로 두고 주 버튼(채움)과 보조 버튼(테두리)을 구분 ② 기간 옆의 빠른 선택 버튼(최근 1·3·6개월) ③ 「입찰마감제외」 체크 ④ 표의 한 칸에 게시일과 마감일을 함께 표시 ⑤ 공고명 링크와 진행 상태 열 ⑥ 표 위 툴바(엑셀 내려받기·표시 개수)|상단 메뉴 구성과 문구, 파란 계열 색, 마스코트·아이콘·사이드 퀵메뉴, 로고, 글꼴, 표의 열 이름·순서·배치 그대로, 입력 칸의 문구|3-3절의 조건 패널·툴바·목록(내 색 청록·주황, 내 문장, 내 열 구성). 채택: ①③④⑤와 툴바 개념. 보류(F-02 이후): ② 빠른 선택 버튼, 엑셀 내려받기, 표시 개수 선택|

## 바꾸기 전·후와 선택 이유

- **바꾸기 전**: 빈 양식(참고 원칙·랜딩 요소·앱 화면 칸이 모두 비어 있음).
- **바꾼 뒤**: 원칙 4행, 랜딩 6요소, 앱 상태 와이어프레임 6개(R-01·R-02·R-03a·R-03b·R-03c·R-04)와 참고 화면을 반영한 최종 배치(3-3절, R-05 자료 선택 포함), 화면 요소↔R-ID↔확인 방법 표를 채웠다.
- **선택 이유**: ① 저니맵 최저점(결과 확인)에서 이탈하지 않게 R-03b 안내 상자에 조건 완화 안내와 링크를 둔다. ② 수강생이 고른 핵심 가치(바로 공유)를 R-04 버튼으로 눈에 띄게 한다. ③ 랜딩의 숫자 2개는 `data_sources.md`에서 직접 확인한 값만 쓴다. ④ 미측정 수치(시간 절감)는 화면에 쓰지 않는다.
- 다음 M6-01에서 이 설계대로 **원본 HTML/CSS/SVG**로 `index.html`(랜딩)을 구현하고, M6-02에서 `app.html`·`app.js`·`data.js`를 구현합니다.
