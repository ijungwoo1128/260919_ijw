# MVP 범위와 RICE — 먼저 만들 기능 하나 (M5-07)

MVP 목표는 사용자가 핵심 일을 한 번 끝낼 수 있는 가장 작은 작동 버전입니다. 버튼 하나만 보이는 화면과 구분합니다.

> 입력: `journey_map.md`(기회·최저점) · `lean_canvas.md` · `topic.md` · `feedback_log.md`. 표의 R을 뺀 I·C·E는 모두 **추정**입니다. RICE는 우선순위 도구이며 실제 사업 성공을 증명하지 않습니다. [원안: Intercom](https://www.intercom.com/blog/rice-simple-prioritization-for-product-managers/)

1. 저니맵의 기회와 최저점에서 기능 후보 4개를 뽑았고, 2026-09-19에 기존 수집 자료(busan-bid-pilot)를 알게 되어 후보 E를 추가했습니다. (자동 수집·발송은 서버가 필요해 후보에서 뺐습니다.)
2. 후보별로 RICE를 비교합니다. 점수 = R × I × C ÷ E. C는 50%=0.5처럼 계산하고 E는 「제대로 만들 때」 인시입니다.
3. **오늘 수업판**은 한 화면(약 45분)으로 줄입니다. 조건 3개(키워드·금액 범위·기준일), 134건 목록, 「메일 본문 복사」 버튼까지만 만듭니다.

|후보|사용자 문제|R: 한 달 사용 횟수|I|C: 비율|E: 인시|점수|근거/추정·의존성|선정|
|---|---|---:|---:|---:|---:|---:|---|---|
|A. 조건 검색·목록|주 2회 여러 사이트를 직접 돌아 발견이 늦다(저니맵 단계 1~3)|8|3|0.5|8|**1.5**|R: 월·목 주 2회×4주(수강생 진술 E-07에서 계산). I·E: 추정. C: ★ 가정(공고가 충분히 담기는가) 미검증이라 0.5. 선행 없음|F-01에 포함|
|B. New·계속 표시|새로 볼 것과 이어지는 것이 섞여 매번 다시 훑는다(단계 3, 수강생 요구)|8|2|0.5|6|**1.33**|R: 위와 같음. I·E: 추정. C: 변경·재공고는 134건 중 12건(9%)뿐이라 0.5. 선행: A|F-02로 미룸|
|C. 「자료에 없음」 화면|결과가 비면 사이트를 다시 다 돌아야 한다(최저점)|8|1|0.5|3|**1.33**|R: 결과가 비는 횟수는 미측정이라 최대 8회로 둠(추정). I·C·E: 추정. 선행: A|최소판을 R-03b에 포함|
|D. 메일 본문 복사|화·금에 공고를 옮겨 적어 배포한다(단계 4)|8|3|0.8|4|**4.8**|R: 화·금 배포 주 2회×4주(E-07). I·E: 추정. C: 수강생이 「바로 공유」를 핵심으로 선택(E-07)해 0.8. **선행: A (목록이 있어야 공유할 공고가 있음)**|F-01에 포함|
|E. 기존 수집 자료 연결(비공개 「자료 선택」)|공개 자료(도시공사)는 고객 업무와 거리가 멀어 목록이 비기 쉽다(최저점)|8|3|0.8|6|**3.2**|R: 위와 같음. I·E: 추정. C: 실제 자료의 근거가 있어 0.8(D5: 180일 38건, 수학여행 11건, 사실). 선행: A. 위험: 스크랩 재배포 조건 미확인(내 PC 전용으로 완화), 오늘 수집 0건(D7)|F-01에 포함|
|**F-01 = A + D + E (선택)**|찾기에서 공유까지 한 번 끝내되, 내 PC에서는 고객 업무에 맞는 실제 자료로 본다|8|3|0.5|18|**0.67**|E는 8+4+6. C는 묶음 중 낮은 쪽(A)의 0.5|**오늘 만듦**|

**계산식** (모두 추정. R은 진술에서 계산)
- A: 8회 × 3 × 0.5 ÷ 8시간 = 1.5점
- B: 8회 × 2 × 0.5 ÷ 6시간 = 1.33점
- C: 8회 × 1 × 0.5 ÷ 3시간 = 1.33점
- D: 8회 × 3 × 0.8 ÷ 4시간 = 4.8점
- E: 8회 × 3 × 0.8 ÷ 6시간 = 3.2점
- F-01(A+D+E): 8회 × 3 × 0.5 ÷ 18시간 = 0.67점

## 점수와 별개로 확인한 것

|후보|서버·키 없이 한 화면|topic.md 데이터 사용|가장 위험한 가정(★) 확인|
|---|---|---|---|
|A|✅|✅ 도시공사 134건|✅ 결과가 얼마나 나오는지 직접 보게 됨|
|B|✅|✅ 공고상태·게시일|❌ 있는 공고를 더 잘 보여 줄 뿐|
|C|✅|❌ 링크 중심|부분: 빈 결과 처리|
|D|✅|✅|❌ 목록이 있어야 의미가 있음|
|E|✅ (`data_pilot.js`는 내 PC에서만 있고 없어도 동작)|✅ 기존 수집 자료(비공개)|✅ 고객 업무에 가까운 자료로 ★ 가정을 확인|
|**F-01(A+D+E)**|✅|✅|✅ A·E가 확인하고 D가 공유 가치를 확인|

**선택**
- 점수 1등 기능: **D. 메일 본문 복사** (4.8점)
- 오늘 선택한 기능(F-01): **A + D + E 묶음 — 조건으로 공고를 걸러 보고(공개=도시공사, 내 PC=기존 수집 자료 선택) 고른 공고를 메일 본문으로 복사**
- 선택 이유(1등과 다르면 왜 다른지): 1등 D는 A 없이는 쓸 수 없어(선행) A를 함께 넣었다. A는 저니맵 최저점(결과 확인)과 ★ 가정을 확인하는 기능이고, D는 수강생이 고른 핵심 가치(바로 공유)다. 둘을 묶어야 「찾기 → 공유」를 한 번 끝낼 수 있어 MVP 조건에 맞는다. 묶음 점수(0.67)는 낮아 보이지만 C(확신)가 ★ 가정에 묶여 있고 E(작업량)가 커져서이며, 이 가정을 오늘 확인한다.
- **2026-09-19 갱신(기존 수집 자료·참고 화면)**: 수강생이 이미 만들어 둔 수집 자료(D5)를 알려 줘서 「둘 다 지원(도시공사 공개 + 기존 자료 비공개)」을 선택했고, 나라장터 목록 화면의 「입찰마감제외」 체크와 배치를 참고해 「마감·개찰 지난 공고 포함」 옵션(기본은 지난 공고를 뺌)을 F-01에 넣었다. 작업량이 커진 만큼 오늘 수업판은 「자료 선택」 라디오와 어댑터 함수까지만 만든다.
- 오늘 미룬 기능: **F-02 New·계속 표시**(취소 → 재공고·변경 → New → 계속 규칙은 `feedback_log.md`에 기록) · 자동 수집·발송 · 교육청·산하기관·일반기업 원자료 연동 · 조달청 API(키 필요) · 회신 집계 · 기존 수집 자료의 공개 배포 · 수집기와의 자동 연동 · 엑셀 내려받기·게시일 빠른 선택·페이지 크기(참고 화면의 기능) · 로그인·결제

- 이번 핵심 기능: 조건 검색으로 공고를 걸러 보고 메일 본문을 만든다
- 사용자가 완료할 행동: 조건을 넣고 → 목록에서 공고를 고르고 → 「메일 본문 복사」를 누른다
- **입력 → 처리 → 결과**: 키워드·금액 범위·기준일 입력 → 선택한 자료(도시공사 134건, 내 PC에서는 기존 수집 자료 38건도 선택 가능)에서 조건에 맞는 공고를 걸러 「개찰까지 남은 일수(개찰예정일 − 기준일)」를 계산 → 공고 목록과 고른 공고의 메일 본문(결과가 없으면 「자료에 없음」과 교육청·학교장터·나라장터 링크)
- 이번에 제외할 기능: 위 「오늘 미룬 기능」. 지난 공고는 F-01에서 기본으로 뺀다(진행 중만 보임). 「마감·개찰 지난 공고 포함」 체크(기본 꺼짐)를 켜면 「개찰 지남」으로 표시해 함께 보여 준다. New·계속 같은 표시 규칙은 F-02다.
- 완료 기준: R-01 정상(키워드 「어린이」 → 놀이시설·어린이집 2건 표시) · R-02 빈값(조건이 모두 비면 안내) · R-03 오류(R-03a 잘못된 입력 · R-03b 자료에 없음+교육청 링크 · R-03c 자료 불러오기 실패) · R-04 메일 본문 복사 · R-05 자료 선택(기존 수집 자료, 내 PC 전용)을 직접 눌러 확인한다. 상세는 `requirements.md`(M5-08).
- 필요한 선행 연결: `data/raw/busan_urban_bids_2025.csv`를 맨 위 `data.js`로 변환한다. 원본은 CP949 인코딩이고, 공고명에 쉼표가 든 8행이 따옴표로 묶여 있으며, 입찰추정가격 2행이 `2.9671E+11`처럼 과학 표기로 깨져 있다(`data_sources.md`). 「교육」은 공고명에 없어 동의어(교육·강사·연수·수련·어린이·놀이·어린이집·유치원·학교)를 함께 검색한다. 기존 수집 자료는 통합본 `data.json`에서 필요한 열만 골라 `data_pilot.js`(내 PC 전용, 저장소·배포에 넣지 않음)로 변환한다. 담당자(`author`)·연락처(`phone`)를 포함하므로 공개 파일로 옮기지 않는다.
- 고객에게 보여 줄 가장 작은 화면과 확인 질문 1개: `feedback_log.md`의 「고객에게 보여 줄 가장 작은 화면과 확인 질문 1개」를 따른다.
